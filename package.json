{
  "scripts": {
    "bootstrap": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -n frontend,backend \"cd frontend && npm run dev\" \"cd backend && npm run dev\"",
    "build": "rm -rf ./backend/dist && mkdir ./backend/dist && npm run build --prefix frontend && npm run build --prefix backend",
    "deploy": "cd backend && ./deploy.sh",
    "deploy:nfs": "npm run build && npm run deploy",
    "start": "NODE_ENV=production npm run start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
